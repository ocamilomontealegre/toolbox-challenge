// App Config
export const appConfig = {
  port: 3000,
}

// Toolbox API Config
export const toolboxApiConfig = {
  baseUrl: "https://echo-serv.tbxnet.com/v1/secret",
  secretKey: "Bearer aSuperSecretKey",
  resources: { 
    resource1: "files",
    resource2: "file",
  }
}

